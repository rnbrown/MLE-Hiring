# Data assets for the take-home

- **`merchants.csv`** — Sample merchant list. Ingest and validate schema; use for features and model training.
- **`sample_merchant_summary.pdf`** — Sample merchant terms/summary. Process **asynchronously** and extract text for the pipeline.
- **`simulated_api_contract.json`** — JSON Schema for the mock API you must implement and call (returns internal risk flags and transaction summary per merchant).
- **`simulated_api_example_response.json`** — Example response matching the contract (for reference).
